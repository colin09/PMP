
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>找回密码</title>
</head>
<body>
    <div class="ptb10 bb">
        <span>会员中心 > 我的信息 > 找回密码</span>
    </div>
    <div>
        <h3 class="ptb10">找回密码</h3>
    </div>
    <div style="padding:15px">
        <div class="form-horizontal">
            <div class="form-group">
                <label for="user" class="col-sm-2 control-label">账  号：</label>
                <div class="col-sm-10">
                    <input type="password" class="form-control" name="user" id="user" value="" placeholder="账号" required="required">
                </div>
            </div>
            <div class="form-group">
                <label for="pwd" class="col-sm-2 control-label">重置密码：</label>
                <div class="col-sm-10">
                    <input type="password" class="form-control" name="pwd" id="pwd" placeholder="重置密码" required="required">
                </div>
            </div>
            <div class="form-group">
                <label for="Codes" class="col-sm-2 control-label">验证码：</label>
                <div class="col-sm-10">
                    <input type="password" class="form-control" name="Codes" id="Codes" placeholder="验证码" required="required">
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button class="btn btn_blue">修改</button>
                </div>
            </div>
        </div>
    </div>
    @section scripts{
        <script type="text/javascript">
            $(".btn_blue").click(function () {
                //if ($("#user").val() =="") {
                //    layer.msg("请输入账号！"); return;
                //}
                //if ($("#pwd").val() == "") {
                //    layer.msg("请输入密码！"); return;
                //}
                //if ($("#Codes").val() == "") {
                //    layer.msg("请输入验证码！"); return;
                //}

                //var data = { oldpwd: $("#oldpwd").val(), newpwd: $("#newpwd").val() }
                //$.get("/Account/UpdatePassWord", data, function (res) {
                //    if (res.IsSuccess == "False") {
                //        layer.msg(res.error);
                //    } else {
                //        layer.msg("修改成功。");
                //    }
                //}, "json")
            });
        </script>
    }
</body>
</html>
